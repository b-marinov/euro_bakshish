# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must always use LF line endings
*.sh text eol=lf

# Batch scripts must use CRLF on all platforms
*.bat text eol=crlf
*.cmd text eol=crlf

# Python files should use LF
*.py text eol=lf

# Ensure specific shell scripts always use LF
entrypoint.sh text eol=lf
validate-docker-setup.sh text eol=lf
